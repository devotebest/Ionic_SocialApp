'use strict';

describe('Service: api/configs', function () {

  // load the service's module
  beforeEach(module('2centsApp'));

  // instantiate service
  var api/configs;
  beforeEach(inject(function (_api/configs_) {
    api/configs = _api/configs_;
  }));

  it('should do something', function () {
    expect(!!api/configs).toBe(true);
  });

});
